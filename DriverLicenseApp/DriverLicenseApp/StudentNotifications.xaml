<Window x:Class="DriverLicenseApp.StudentNotifications"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DriverLicenseApp"
        mc:Ignorable="d"
        Title="Notification" Height="560" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="AliceBlue">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Notifications" FontSize="20" FontWeight="Bold" Foreground="DarkBlue"
               HorizontalAlignment="Center" Margin="0,0,0,10" Grid.Row="0"/>

        <!-- Bộ lọc trạng thái -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10" Grid.Row="1">
            <TextBlock Text="Filter by Status:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="cbStatusFilter" Width="150">
                <ComboBoxItem Content="All" IsSelected="True" Tag="2"/>
                <ComboBoxItem Content="Unread" Tag="0"/>
            </ComboBox>
            <Button Margin="10 0 0 0" Width="50" x:Name="btnFilter" Click="btnFilter_Click">Filter</Button>
        </StackPanel>

        <!-- Danh sách đăng ký -->
        <DataGrid x:Name="dgNotifications" AutoGenerateColumns="False" IsReadOnly="True"
              Grid.Row="2" Height="380" Background="White" BorderBrush="Gray" BorderThickness="1"
              SelectionChanged="dgNotifications_SelectionChanged">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="White" />
                    <Style.Triggers>
                        <!-- Nếu IsRead == false, đổi nền thành LightBlue -->
                        <DataTrigger Binding="{Binding IsRead}" Value="False">
                            <Setter Property="Background" Value="LightBlue" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Notification ID" Binding="{Binding NotificationId}" Width="100"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="400"/>
                <DataGridTextColumn Header="SentDate" Binding="{Binding SentDate}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
